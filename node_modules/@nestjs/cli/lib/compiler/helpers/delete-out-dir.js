"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deleteOutDirIfEnabled = deleteOutDirIfEnabled;
const promises_1 = require("fs/promises");
const path_1 = require("path");
const get_value_or_default_1 = require("./get-value-or-default");
async function deleteOutDirIfEnabled(configuration, appName, dirPath, tsOptions) {
    const isDeleteEnabled = (0, get_value_or_default_1.getValueOrDefault)(configuration, 'compilerOptions.deleteOutDir', appName);
    if (!isDeleteEnabled) {
        return;
    }
    const resolvedOutDir = resolvePathInsideProject(dirPath, 'outDir');
    const resolvedTsBuildInfoFile = tsOptions?.tsBuildInfoFile
        ? resolvePathInsideProject(tsOptions.tsBuildInfoFile, 'tsBuildInfoFile')
        : undefined;
    await (0, promises_1.rm)(resolvedOutDir, {
        recursive: true,
        force: true,
    });
    if (resolvedTsBuildInfoFile) {
        await (0, promises_1.rm)(resolvedTsBuildInfoFile, {
            force: true,
        });
    }
}
function resolvePathInsideProject(pathToDelete, propertyName) {
    const projectRoot = process.cwd();
    const resolvedPath = (0, path_1.resolve)(projectRoot, pathToDelete);
    const relativePath = (0, path_1.relative)(projectRoot, resolvedPath);
    const isProjectRoot = relativePath === '';
    const isOutsideProject = relativePath.startsWith('..') || (0, path_1.isAbsolute)(relativePath);
    if (isProjectRoot || isOutsideProject) {
        throw new Error(`Refusing to delete "${propertyName}" path outside of or equal to the project directory: ${pathToDelete}`);
    }
    return resolvedPath;
}
